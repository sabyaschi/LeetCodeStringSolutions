class Solution {
    public boolean checkString(String s) {
        // if(s.length() == 2){
        // if(s.charAt(0) == 'a' && s.charAt(1) == 'b'){
        // return true;
        // }
        // else{
        // return false;
        // }
        // }
        // for (int i = 0; i < s.length() - 1; i++) {
        //     if (i == 0 && (s.charAt(i) - 'a') > (s.charAt(i + 1) - 'a')) {
        //         return false;
        //     } else if (i == s.length() - 1 && (s.charAt(i) - 'a') > (s.charAt(i - 1) - 'a')) {
        //         return false;
        //     } else if ((s.charAt(i) - 'a') > (s.charAt(i + 1) - 'a')) {
        //         return false;
        //     }
        // }
           for (int i = 0; i < s.length() - 1; i++){
            if(s.charAt(i) == 'b' && s.charAt(i+1) == 'a'){
                return false;
                }
           }
        return true;
    }
}
