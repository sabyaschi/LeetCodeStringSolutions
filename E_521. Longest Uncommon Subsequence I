class Solution {
    public int findLUSlength(String a, String b) {
        int len1 = a.length();
        int len2 = b.length();
        if(!a.equals(b)){
            return len1 > len2 ? len1 : len2;
        }
        return -1;
    }
}
